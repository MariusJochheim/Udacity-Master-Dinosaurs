## Task Checklist

- [ ] Implement `Dino` constructor + prototypes (`compareWeight`, `compareHeight`, `compareDiet`, `randomFact`).
- [ ] Implement `Human` constructor + form IIFE to capture input.
- [ ] Fetch `dino.json`, map to `Dino` instances, ensure total objects = 9 (7 dinos + pigeon + human), shuffle list.
- [ ] Insert human at index 4, build tiles (species/name, image, fact only for dinos), append to `#grid`.
- [ ] Hide `#dino-compare` form after generation.
- [ ] Remove any `console.log` and ensure no errors occur on click.
- [ ] Apply Udacity JS style: descriptive names, functions declared before use, brief comments for non-obvious logic.
- [ ] Manual verify: click “Compare Me!”, form hides, 9 tiles appear, human centered, pigeon fact fixed, random facts vary after refresh/new data.
- [ ] Add input validation and basic error messaging.
- [ ] Add regenerate/reset button to re-run without page refresh.
